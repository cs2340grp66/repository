<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weichen.water_report">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controllers.InitialActivity"
            android:label="Water Report"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controllers.LoginActivity" />
        <activity android:name=".controllers.WelcomActivity" />
        <activity android:name=".controllers.RegisterActivity" />
        <activity android:name=".controllers.ProfileActivity" />
        <activity android:name=".controllers.PersonalProfileActivity" />
        <activity android:name=".controllers.SubmitReport" />
        <activity android:name=".controllers.ViewReports" />
        <activity android:name=".controllers.ReportDetailActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAxvFj7ZTdPuhuhAYAB55jEXRqUg010JMU" />

        <activity
            android:name=".controllers.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".controllers.Worker_Welcome_Activity" />
        <activity android:name=".controllers.Manager_Welcome_Activity"></activity>
    </application>

</manifest>